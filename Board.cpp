#include <string>
#include <iostream>
#include "Direction.hpp"
#include "Board.hpp"
#include <map>
using namespace std;

//Use map to rows.
namespace ariel {
            void Board :: post(uint row, uint col, Direction dir, string str){}
            string Board :: read(uint row, uint col, Direction dir, uint len){return "hello";}
            void Board :: show(){}
    
};